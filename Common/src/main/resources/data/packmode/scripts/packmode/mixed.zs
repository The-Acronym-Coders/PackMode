#packmode normal expert

// Multiple packmodes supported, if any of them is the current one then the script is loaded.
// A script with no preprocessor is loaded unconditionally.

println("Mixed packmode!");